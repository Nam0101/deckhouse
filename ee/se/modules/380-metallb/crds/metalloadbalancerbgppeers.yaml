---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    module: metallb
    backup.deckhouse.io/cluster-config: "true"
  name: metalloadbalancerbgppeers.network.deckhouse.io
spec:
  group: network.deckhouse.io
  names:
    kind: MetalLoadBalancerBGPPeer
    listKind: MetalLoadBalancerBGPPeerList
    plural: metalloadbalancerbgppeers
    singular: metalloadbalancerbgppeer
    shortNames:
      - mlbbp
  scope: Cluster
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: MetalLoadBalancerBGPPeer is the Schema for the metalloadbalancerbgppeers API
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              type: object
              required:
                - peerAddress
                - peerASN
                - myASN
              properties:
                peerAddress:
                  type: string
                  description: |-
                    The IP address of the BGP peer.
                peerPort:
                  type: integer
                  description: |-
                    The port of the BGP peer.
                peerASN:
                  type: integer
                  description: |-
                    The Autonomous System Number (ASN) of the BGP peer.
                myASN:
                  type: integer
                  description: |-
                    The local Autonomous System Number (ASN) used for peering.
                routerID:
                  type: string
                  description: |-
                    BGP router ID to advertise to the peer.
                holdTime:
                  type: string
                  description: |-
                    The requested BGP hold time.
                passwordSecretRef:
                  description: |-
                    Reference to the Secret containing the password for BGP session authentication.

                    **Important:** the target Secret MUST have the label `network.deckhouse.io/metallb-bgp-password: "true"`.
                  type: object
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                    - name
                    - namespace
                sourceAddresses:
                  type: array
                  items:
                    type: object
                    properties:
                      nodeName:
                        type: string
                      address:
                        type: string
                    required:
                      - nodeName
                      - address
                  description: |-
                    Per-node override of the local IP address used for peering.
                bfd:
                  type: object
                  properties:
                    receiveInterval:
                      type: integer
                    transmitInterval:
                      type: integer
                    detectMultiplier:
                      type: integer
                    echoInterval:
                      type: integer
                    echoMode:
                      type: boolean
                    passiveMode:
                      type: boolean
                    minimumTtl:
                      type: integer
                  description: |-
                    BFD configuration for the BGP peer.
            status:
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}

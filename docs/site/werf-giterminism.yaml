giterminismConfigVersion: 1
config:
  goTemplateRendering:
    allowEnvVariables: [ /BASE_.+/, /CI_.+/, "WERF_ENV" , /DOC_API_.+/, "SOURCE_REPO" ]
  stapel:
    git:
      allowBranch: true
  secrets:
    allowValueIds:
    - GOPROXY
    - SOURCE_REPO
